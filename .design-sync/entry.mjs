// Intentionally empty: HivePulse has no React component library to bundle.
// An explicit entry with no exports puts the converter in tokens-only mode instead of
// scanning components/, whose files are data-fetching page sections, not reusable parts.
export {};

<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext (plain HTTP) traffic is only ever needed against the emulator's
  host-loopback alias (10.0.2.2) or a local dev server (localhost/127.0.0.1)
  while BASE_URL points at a local backend during development. Every real
  build target (staging, production) is HTTPS. Scoping this — instead of the
  previous blanket android:usesCleartextTraffic="true" — means a release
  build shipped without BASE_URL properly injected fails closed instead of
  silently allowing plaintext HTTP anywhere.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>

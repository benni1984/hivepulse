# Testing Rules

- Every new backend endpoint needs a test in `backend/tests/`. Run `pytest` from `backend/` before done.
- Every new web component, hook, page, or utility needs a test in `__tests__/`. Run `npm test` before done.
- No PR is complete without tests for all new code. If tests are impossible (e.g. pure CSS), state why explicitly.
